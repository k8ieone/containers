---
kind: pipeline
type: docker
name: default

steps:
- name: test
  image: k8ieone/arch-builder:base
  commands:
  - build-aur yay
  - find /home/builder/built/ -iname *pkg.tar* -exec pacman -U --noconfirm "{}"  \;
